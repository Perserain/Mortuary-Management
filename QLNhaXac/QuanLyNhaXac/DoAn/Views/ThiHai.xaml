<UserControl x:Class="DoAn.QuanLy.ThiHai"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:DoAn.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="650" d:DesignWidth="850">

    <UserControl.DataContext>
        <vm:ThiHaiViewModel/>
    </UserControl.DataContext>

    <Grid Background="#2E4053" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="QUẢN LÝ THI HÀI" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <GroupBox Grid.Row="1" Header="Thông Tin Tử Thi" FontSize="13" Margin="0,0,0,10" Padding="10" Background="White" HorizontalAlignment="Stretch">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Mã Thi Hài:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <TextBox Text="{Binding SelectedThiHai.MaTH, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Height="25" Margin="0,5,15,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Họ Tên:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <TextBox Text="{Binding SelectedThiHai.HoTenTH, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="3" Height="25" Margin="0,5,0,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Ngày Sinh:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding SelectedThiHai.NgaySinh, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Height="25" Margin="0,5,15,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Ngày Mất:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding SelectedThiHai.NgayMat, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" Height="25" Margin="0,5,0,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Giới Tính:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <ComboBox Text="{Binding SelectedThiHai.GioiTinh, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Height="25" Margin="0,5,15,5" HorizontalAlignment="Stretch">
                    <ComboBoxItem Content="Nam"/>
                    <ComboBoxItem Content="Nữ"/>
                    <ComboBoxItem Content="Chưa rõ"/>
                </ComboBox>

                <StackPanel x:Name="StPanelNut"  Grid.Row="2" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="THÊM" Command="{Binding ThemCommand}" Width="70" Height="30" Margin="5,0" Background="#2E8B57" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
                    <Button Content="SỬA" Command="{Binding SuaCommand}" Width="70" Height="30" Margin="5,0" Background="#FFD700" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
                    <Button Content="XÓA" Command="{Binding XoaCommand}" Width="70" Height="30" Margin="5,0" Background="#DC143C" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column"
                  ItemsSource="{Binding DanhSachThiHai}" SelectedItem="{Binding SelectedThiHai}">
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding XemChiTietCommand}"/>
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã Thi Hài" Binding="{Binding MaTH}" Width="80"/>
                <DataGridTextColumn Header="Họ Tên" Binding="{Binding HoTenTH}" Width="*"/>
                <DataGridTextColumn Header="Ngày Sinh" Binding="{Binding NgaySinh, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Ngày Mất" Binding="{Binding NgayMat, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Giới Tính" Binding="{Binding GioiTinh}" Width="80"/>
                <DataGridTextColumn Header="Trạng Thái" Binding="{Binding NhomTuoi}" Width="120" FontWeight="Bold"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel x:Name ="StPanelChucNang" Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Left" Margin="48,12,0,12">
            <Button Content="TÌM THI HÀI SÓT" Command="{Binding TimSotCommand}" Width="140" Height="35" Margin="5,0" Background="#FF4500" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="NHẬP TỪ FILE" Command="{Binding NhapTuFileCommand}" Width="120" Height="35" Margin="5,0" Background="#32CD32" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="XUẤT EXCEL" Command="{Binding XuatExcelCommand}" Width="120" Height="35" Margin="5,0" Background="#008080" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="THI HÀI QUÁ HẠN" Command="{Binding ThanhLyCommand}" Width="120" Height="35" Margin="5,0" Background="DarkGray" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>
        <TextBlock x:Name="demThiHai" Text="{Binding CountThiHai}" HorizontalAlignment="Left" Margin="622,18,0,0" Grid.Row="3" TextWrapping="Wrap" VerticalAlignment="Top" FontSize="15" FontWeight="Bold" Foreground="White"/>
    </Grid>
</UserControl>