<UserControl x:Class="DoAn.QuanLy.SuDung"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:DoAn.ViewModel"
             Background="#2E4053"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="850">
    <UserControl.DataContext>
        <vm:SuDungViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="QUẢN LÝ DỊCH VỤ ĐÃ MUA" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <GroupBox Header="Nhập thông tin giao dịch" FontSize="13" Grid.Row="1" Margin="0,0,0,10" Padding="10" Background="White" HorizontalAlignment="Stretch">
            <Grid Height="100">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Mã Thi Hài:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Bottom" Margin="0,0,5,9" FontWeight="Bold"/>
                <TextBox Text="{Binding SelectedSuDung.MaTH, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="0,5,15,5" Height="25"/>

                <TextBlock Text="Mã Dịch Vụ:" Grid.Row="0" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <TextBox Text="{Binding SelectedSuDung.MaDV, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="3" Margin="0,5,0,5" Height="25"/>

                <TextBlock Text="Ngày Sử Dụng:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <DatePicker SelectedDate="{Binding SelectedSuDung.NgaySD, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="0,5,15,5" Height="25"/>
                <TextBlock Text="Ghi Chú:" Grid.Row="1" Grid.Column="2" VerticalAlignment="Center" Margin="0,5,5,5" FontWeight="Bold"/>
                <TextBox Text="{Binding SelectedSuDung.GhiChu, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="3" Margin="0,5,0,5" Height="25"/>

                <StackPanel Grid.Row="1" Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,40,0,-25">
                    <Button Content="THÊM" Command="{Binding ThemCommand}" Width="70" Height="30" Margin="5,0" Background="#2E8B57" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
                    <Button Content="XÓA" Command="{Binding XoaCommand}" Width="80" Height="30" Margin="5,0" Background="#DC143C" Foreground="Black" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <DataGrid Grid.Row="2" AutoGenerateColumns="False" CanUserAddRows="False" Background="White" HeadersVisibility="Column" ItemsSource="{Binding DanhSachSuDung}" SelectedItem="{Binding SelectedSuDung}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Mã TH" Binding="{Binding MaTH}" Width="80"/>
                <DataGridTextColumn Header="Tên Người Nằm" Binding="{Binding TenTH}" Width="150"/>
                <DataGridTextColumn Header="Mã DV" Binding="{Binding MaDV}" Width="80"/>
                <DataGridTextColumn Header="Tên Dịch Vụ" Binding="{Binding TenDV}" Width="150"/>
                <DataGridTextColumn Header="Giá Tiền" Binding="{Binding GiaTien, StringFormat={}{0:N0} VNĐ}" Width="110"/>
                <DataGridTextColumn Header="Ngày Đăng Ký" Binding="{Binding NgaySD, StringFormat=dd/MM/yyyy}" Width="120"/>
                <DataGridTextColumn Header="Ghi Chú" Binding="{Binding GhiChu}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,5">
            <Button Content="TÍNH TỔNG TIỀN" Command="{Binding TinhTongTienCommand}" Width="150" Height="35" Margin="5,0" Background="#FFA500" Foreground="WHITE" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="NHẬP TỪ FILE" Command="{Binding NhapTuFileCommand}" Width="160" Height="35" Margin="5,0" Background="#32CD32" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
            <Button Content="XUẤT BÁO CÁO EXCEL" Command="{Binding XuatExcelCommand}" Width="180" Height="35" Margin="5,0" Background="#008080" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
        </StackPanel>
        <TextBlock x:Name="tongTien" Text="{Binding TongTien}" HorizontalAlignment="Left" Margin="680,0,0,0" Grid.Row="3" TextWrapping="Wrap" VerticalAlignment="Center" FontSize="15" FontWeight="Bold" Foreground="White"/>
    </Grid>
</UserControl>